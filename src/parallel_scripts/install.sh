# install fighting game and ai
bash parallel_scripts/first_connect.sh
read -s -n1 -p "等到任务完成后，再按任意键继续 ... "

bash parallel_scripts/first_connect.sh
read -s -n1 -p "等到任务完成后，再按任意键继续 ... "

bash parallel_scripts/copy_file.sh
read -s -n1 -p "等到任务完成后，再按任意键继续 ... "

bash parallel_scripts/copy_file.sh
read -s -n1 -p "等到任务完成后，再按任意键继续 ... "

bash parallel_scripts/install_env.sh
read -s -n1 -p "等到任务完成后，再按任意键继续 ... "

bash parallel_scripts/install_env.sh
read -s -n1 -p "等到任务完成后，再按任意键继续 ... "

bash parallel_scripts/install_env_py.sh
read -s -n1 -p "等到任务完成后，再按任意键继续 ... "

bash parallel_scripts/install_env_py.sh
read -s -n1 -p "等到任务完成后，再按任意键继续 ... "

bash parallel_scripts/xvfb.sh
read -s -n1 -p "等到任务完成后，再按任意键继续 ... "

bash parallel_scripts/xvfb.sh
read -s -n1 -p "等到任务完成后，再按任意键继续 ... "
